import styled from 'styled-components'

const ModalHolder = styled.main`
  position: fixed;
  top: 0px;
  left: 0px;
  width: 100%;
  height: 100%;
  display: grid;
  place-items: center;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1;
`

export default ModalHolder
